{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.4.0"
  }
}
